# fs_shell headless smoke
# Prefer: python scripts/fs_shell.py --reset -y --host-cwd test-sandbox scripts/fs_shell_smoke.txt
# (host cwd is untracked ./test-sandbox at repo root — not scripts/)
echo -- smoke start --
pwd
pwd --local
ls /lfs0
ls -l /lfs0
# Seed a small payload into the sandbox, then put/get round-trip
# (script path is relative to --host-cwd when using put with a basename)
# Copy of this file's marker only: write via host before put if needed.
# For smoke we put a tiny file created in sandbox by the runner, or use
# a path under host cwd. Place smoke_payload.txt there, or put this script:
put -y smoke_payload.txt /lfs0/fs_shell_smoke.txt
ls -l /lfs0
get -y /lfs0/fs_shell_smoke.txt fs_shell_smoke_roundtrip.txt
echo -- smoke done --
exit
